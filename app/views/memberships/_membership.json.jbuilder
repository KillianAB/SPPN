json.extract! membership, :id, :first_name, :last_name, :diplome, :age, :activity, :massif, :periode, :passmorgiou, :passsormiou, :siret, :description, :user_id, :photo, :created_at, :updated_at
json.url membership_url(membership, format: :json)
